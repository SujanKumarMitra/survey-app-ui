import FormDescription from './components/FormDescription/FormDescription.lazy';
import ResponseCard from './components/ResponseCard/ResponseCard.lazy';
const App = (props) => {
    return (
        <>
            <FormDescription />
        </>
    );
}

export default App;
