/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import ResponseCard from './ResponseCard';

storiesOf('ResponseCard', module).add('default', () => <ResponseCard />);
