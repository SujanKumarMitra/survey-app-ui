/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import FormResponseError from './FormResponseError';

storiesOf('FormResponseError', module).add('default', () => <FormResponseError />);
