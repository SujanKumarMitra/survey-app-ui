/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import FormDescription from './FormDescription';

storiesOf('FormDescription', module).add('default', () => <FormDescription />);
