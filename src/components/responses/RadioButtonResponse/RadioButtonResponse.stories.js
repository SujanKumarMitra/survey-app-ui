/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import RadioButtonResponse from './RadioButtonResponse';

storiesOf('RadioButtonResponse', module).add('default', () => <RadioButtonResponse />);
